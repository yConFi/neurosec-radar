[project]
name = "neurosec-radar"
version = "0.1.0"
description = "Personal radar for AI, cybersecurity and AI x cybersecurity news"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
